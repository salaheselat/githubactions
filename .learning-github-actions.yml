name: learning-github-actions
on:
  - push
jobs:
  check-bats-version: null
  run-on: ubuntu-latest
  steps:
    - uses:actions/checkout@v2
    - uses:actions/setup-node@v1
    - run:npm install -g bats
    - run:bats -v
